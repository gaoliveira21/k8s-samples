apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: db
  namespace: vote
spec:
  type: ClusterIP
  ports:
    - port: 5432
      name: db-svc
      targetPort: 5432
  selector:
    app: db